import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  screen: {
    width: '100%',
    justifyContent: 'flex-start',
    alignItems: 'center',
  },
  scrollView: {
    flex: 1,
    width: 'auto',
    alignItems: 'center',
    justifyContent: 'flex-start',
  },
});