import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    borderRadius: 50,
    overflow: 'hidden',
  },
  button: {
    width: 50,
    height: 50,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
